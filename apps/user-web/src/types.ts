export type SearchResult = {
  id: string
  name: string
  city: string
  venue_type: string
  capacity: number
  pricing_mode: string
  starting_price_paise: number | null
  cover_photo_url: string | null
}
