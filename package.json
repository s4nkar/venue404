{
  "name": "venue404",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n user,owner,admin -c blue,green,magenta \"pnpm --filter user-web dev\" \"pnpm --filter owner-portal dev\" \"pnpm --filter admin-panel dev\"",
    "dev:all": "concurrently -n docker,user,owner,admin -c yellow,blue,green,magenta \"docker compose up\" \"pnpm --filter user-web dev\" \"pnpm --filter owner-portal dev\" \"pnpm --filter admin-panel dev\"",
    "dev:api": "docker compose up api",
    "api:migrate": "docker compose exec api alembic upgrade head",
    "api:migrate:new": "docker compose exec api alembic revision --autogenerate -m",
    "api:migrate:down": "docker compose exec api alembic downgrade -1",
    "build": "pnpm -r --filter \"./apps/*\" --filter \"!./apps/api\" build",
    "lint": "pnpm -r --filter \"./apps/*\" --filter \"!./apps/api\" lint",
    "typecheck": "pnpm -r --filter \"./apps/*\" --filter \"!./apps/api\" typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "concurrently": "^10.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.0"
  }
}